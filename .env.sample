# Example environment variables
DNS_SERVERS=8.8.8.8,1.1.1.1
PORT=5000

# Optional: Use an external storage URI for rate limiting (prevents in-memory warnings and supports multi-worker setups)
# Example: LIMITER_STORAGE_URI=redis://redis:6379/0
LIMITER_STORAGE_URI=
